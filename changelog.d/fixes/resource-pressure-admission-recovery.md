- **fix(sse):** break the resource-pressure 503 loop that only a process restart could clear — the admission gate read a cached `critical` severity while the only code able to resample it ran downstream of that same gate, so a single transient spike shed every request indefinitely; the gate now drives an active re-sample through `checkResourcePressureGuard()` and downgrades a merely stale cached `critical` to `high`, which never sheds
- **fix(resilience):** escape the critical hysteresis dead zone — a cgroup workingset ratio parked between `recoveryRatio` and `criticalRatio` was neither a critical reconfirmation nor a recovery, latching severity at `critical` forever; `criticalHoldTimeoutMs` (default 3 minutes without a genuinely critical raw sample) now drops it to `high`, which never sheds, letting a real relapse re-escalate in ~2s
- **fix(resilience):** ramp the `resource_pressure` 503 `Retry-After` from a 2s floor to a 15s ceiling as the critical state persists, and carry `pressureReason`, `heapUsedMb`, `cgroupPct` and `criticalForMs` on the shed event so a 503 storm is diagnosable from one log module
