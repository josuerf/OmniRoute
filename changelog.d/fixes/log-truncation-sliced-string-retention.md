- **fix(sse):** stop log truncation from retaining the entire payload it truncates — `String.prototype.slice()` returns a V8 SlicedString that points into the parent and keeps the _whole_ parent alive, so a 64 KB preview of an 8 MB body retained all 8 MB (measured: 8192 KB held by a 64 KB excerpt) across the 17 `cloneBoundedChatLogPayload` call sites per request and the 8 `capMemoryExtractionText` call sites in memory extraction; `truncateChatLogText()` and `capMemoryExtractionText()` now flatten their result through a utf16le round-trip, which copies code units verbatim so lone surrogates survive where a utf8 round-trip would rewrite them to U+FFFD
- **fix(usage):** as a consequence, the completed-detail byte budget now measures what it claims to — `estimateRetainedBytes()` sizes strings with `Buffer.byteLength()`, i.e. the logical length, so while previews were SlicedStrings a 16 MB cap could authorize gigabytes of genuine retention; no change was needed in `completedRequestDetails.ts` itself, the inputs simply stopped lying
